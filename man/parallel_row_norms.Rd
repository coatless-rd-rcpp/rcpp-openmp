% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{parallel_row_norms}
\alias{parallel_row_norms}
\title{Row Euclidean Norms Computed in Parallel with OpenMP}
\usage{
parallel_row_norms(x)
}
\arguments{
\item{x}{A \code{numeric} matrix.}
}
\value{
A \code{numeric} vector with one entry per row of \code{x}, each the square root of
the sum of squares of that row.
}
\description{
Computes the Euclidean (L2) norm of each row of a numeric matrix, with the
rows divided across threads by \emph{OpenMP}.
}
\details{
The \verb{#pragma omp parallel for} directive splits the outer loop over rows
across the available \emph{OpenMP} threads. The directive is only meaningful when
the package is compiled with \emph{OpenMP} support; without it the loop simply
runs serially.

Only the input matrix is read and the pre-allocated output vector is written
inside the parallel region, and each thread writes a distinct index. No \emph{R}
API is touched inside the loop, which is what keeps the routine thread-safe.
}
\examples{
x = matrix(rnorm(20), nrow = 5)
parallel_row_norms(x)

}
